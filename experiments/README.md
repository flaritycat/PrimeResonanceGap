# Experiments

This directory is for computational probes. Computational evidence is **HEURISTIC** unless backed by proof.

Current review anchor:

```text
docs/reviews/Prime_Resonance_Gap_1000_Page_Review.md
```

Experiments may inform the Phase G feasibility gates, but they do not prove
`KernelHolderGate_252`, `FixedRowOnePointPkg_249`, the residual cube endpoint,
or any original theorem.

Potential experiments:

- pair statistics for `P_d(n)`;
- rectangle residual statistics with exact `Sigma_w(d,h)`;
- derivative Fourier fourth moments for `B_d(n)=f(n+d)conj(f(n))`;
- high-count / Bonferroni remainder simulations;
- tail threshold and empty-interval statistics.
- kernel absolute-mass probes for candidate `P_M`;
- one-point boundary interval length and side-row diagnostics.

Do not upgrade experiments to theorems. Apparently this still has to be said.
