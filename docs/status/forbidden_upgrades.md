# Forbidden upgrades

Do not claim any of the following unless a complete proof is supplied.

## Global forbidden upgrades

- Original positive existence problem solved.
- All-alpha no-positive-limit theorem proved.
- Unconditional finite-type theorem proved.
- Metric almost-everywhere theorem implies all-alpha theorem.

## Selector forbidden upgrades

- Smoothed estimate implies sharp actual moving-selector estimate.
- Frozen dyadic selector theorem implies actual selector theorem.
- Bernoulli lift theorem implies actual deterministic selector theorem.

Required actual selector:

```text
chi_alpha(p)=1_{||alpha p||<1/log p}
```

## Gap-object forbidden upgrades

- Fixed-order tuple neutrality implies empty-interval control.
- Global mean of normalized prime gaps implies local tail uniform integrability.
- Bounded clipped-gap estimate controls full unbounded gap estimate.

## Endpoint forbidden upgrades

- Pair-BDH alone implies `CPC_3^sharp`.
- Mean rectangle-HL implies rectangle-BDH.
- Unweighted first-moment HL implies RBDH.
- Raw shifted-pair Fourier smallness without subtracting `kappa_w(d)`.
- `Sigma_w(d,h)` can be replaced pointwise by `kappa_w(d)^2`.
- Generic finite-complexity HL closes the endpoint unless it is anisotropic, W-uniform, model-accurate, and variance-strength.

## Model discipline

Correct pair model:

```text
kappa_w(d)
```

Correct rectangle model:

```text
Sigma_w(d,h)
```

Correct two-rectangle model:

```text
Theta_w(d,h,k)
```

Do not simplify exact local models unless a named averaging lemma justifies the replacement.
