You are continuing the Prime Gap Resonance Project in `flaritycat/PrimeResonanceGap`.

First read:

1. `AGENTS.md`
2. `docs/status/global_status.md`
3. `docs/status/forbidden_upgrades.md`
4. `docs/codex/continuation_protocol.md`
5. `docs/modules/module_178_residual_cube.md`

Then create **Module 179**.

Topic:

```text
Fourier major/minor decomposition for the residual derivative cube endpoint.
```

Required content:

- Status: `STRUCTURAL / EXTRACTION` or `CONDITIONAL`, not `PROVEN`.
- Define `f=nu-1`, `B_d(n)=f(n+d)conj(f(n))`, `c_d`, `B_d^circ`, `Q_d`, and `Omega_w`.
- Derive the spectral target:

```text
(1/D) sum_{D<|d|<=2D} sum_{xi != 0} |widehat{B_d}(xi)|^4 = o(1).
```

- Split `xi` into major and minor arcs.
- State exact local model required on major arcs.
- Explain why ordinary pair-BDH and first-moment HL are insufficient.
- Include edge cases, project-map location, what remains open, and forbidden upgrades.

Never claim the original problem, all-alpha theorem, finite-type theorem, RBDH, CPC, or AU^3 is proved.
